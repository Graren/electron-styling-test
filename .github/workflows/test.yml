name: Test service

on: push

jobs:
  container-job:
    runs-on: ubuntu-latest


    steps:
    - uses: actions/checkout@v1
    - uses: actions/setup-node@v2
        with:
          node-version: '16.8'
    - run: npm install
    - run: npm test